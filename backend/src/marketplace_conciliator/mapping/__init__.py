"""Mapping module — hexagonal boundary (ADR-001).

Responsibility: column-mapping heuristics (OBJ-03), validation on sample,
persistence of confirmed mappings (column_mappings table, T-3.x).

No imports from sibling domain modules (enforced by import-linter).
"""

from __future__ import annotations

__all__: list[str] = []
